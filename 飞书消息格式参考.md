# 飞书消息格式参考与仿客户端设计说明

本文整理飞书即时通讯（IM）里常见的消息收发结构、响应结构和事件结构，供 miniClaw 或自研客户端在“模仿飞书消息能力”时参考。

目标不是完整复刻飞书 OpenAPI，而是抽象出一套兼容飞书核心特性的消息模型：文本、富文本、图片、文件、音视频、卡片、回复、话题、表情回应、已读、编辑/撤回等。

---

## 1. 核心概念

### 1.1 会话 Chat

飞书里的会话可以是单聊或群聊。

常见字段：

```json
{
  "chat_id": "oc_xxx",
  "chat_type": "p2p | group",
  "name": "群名或会话名",
  "avatar": "头像 URL 或资源 key",
  "description": "群描述",
  "owner_id": "ou_xxx",
  "member_count": 12
}
```

建议自研客户端保留：

- `chat_id`：全局唯一会话 ID。
- `chat_type`：区分单聊/群聊。
- `members`：成员列表。
- `last_message`：最近一条消息摘要。
- `unread_count`：未读数。
- `pinned_messages`：置顶消息。

---

### 1.2 消息 Message

飞书消息通常由以下几部分组成：

```json
{
  "message_id": "om_xxx",
  "root_id": "om_xxx",
  "parent_id": "om_xxx",
  "thread_id": "omt_xxx 或 om_xxx",
  "chat_id": "oc_xxx",
  "msg_type": "text",
  "content": "{\"text\":\"hello\"}",
  "sender": {
    "id": "ou_xxx",
    "id_type": "open_id",
    "sender_type": "user | app",
    "tenant_key": "xxx"
  },
  "create_time": "1710000000000",
  "update_time": "1710000000000",
  "deleted": false,
  "updated": false,
  "mentions": []
}
```

注意：飞书 API 里的 `content` 经常是 **字符串化 JSON**，也就是外层是字符串，里面才是真正的消息内容对象。

例如：

```json
{
  "msg_type": "text",
  "content": "{\"text\":\"你好\"}"
}
```

解析时需要：

```python
content_obj = json.loads(message["content"])
```

自研客户端内部可以直接保存为对象，但如果要兼容飞书 API，建议仍支持字符串化 JSON。

---

### 1.3 话题 Thread / 回复 Reply

飞书里普通消息可以被回复，也可以形成话题。

关键字段：

```json
{
  "message_id": "om_reply",
  "root_id": "om_root",
  "parent_id": "om_parent",
  "thread_id": "omt_xxx",
  "chat_id": "oc_xxx"
}
```

含义：

- `message_id`：当前消息 ID。
- `parent_id`：直接回复的上一条消息。
- `root_id`：整棵回复树的根消息。
- `thread_id`：话题 ID，用于获取一个话题下的全部消息。
- 如果没有话题，`root_id` / `parent_id` 可能为空。

自研客户端建议：

- 普通会话按 `chat_id` 拉消息。
- 话题详情按 `thread_id` 拉消息。
- 消息列表里保留 `reply_count` 和 `thread_id`，方便展示“查看 N 条回复”。

---

## 2. 发送消息请求格式

飞书发送消息接口本质上需要三类信息：

1. 发送目标：群聊 `chat_id` 或用户 `open_id`。
2. 消息类型：`msg_type`。
3. 消息内容：`content`。

### 2.1 发送到群聊

逻辑结构：

```json
{
  "receive_id_type": "chat_id",
  "receive_id": "oc_xxx",
  "msg_type": "text",
  "content": "{\"text\":\"你好\"}"
}
```

### 2.2 发送到用户

逻辑结构：

```json
{
  "receive_id_type": "open_id",
  "receive_id": "ou_xxx",
  "msg_type": "text",
  "content": "{\"text\":\"你好\"}"
}
```

### 2.3 发送成功响应

飞书发送成功通常返回简化消息信息：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "message_id": "om_xxx",
    "chat_id": "oc_xxx",
    "create_time": "1710000000000"
  }
}
```

自研客户端建议发送成功后返回：

```json
{
  "ok": true,
  "message": {
    "message_id": "msg_xxx",
    "chat_id": "chat_xxx",
    "sender_id": "user_xxx",
    "msg_type": "text",
    "content": {
      "text": "你好"
    },
    "create_time": 1710000000000
  }
}
```

这样前端可以直接插入消息列表，不需要再二次查询。

---

## 3. 常见 msg_type 与 content 格式

## 3.1 text：普通文本

飞书格式：

```json
{
  "msg_type": "text",
  "content": "{\"text\":\"你好\"}"
}
```

真实内容对象：

```json
{
  "text": "你好"
}
```

支持换行：

```json
{
  "text": "第一行\n第二行"
}
```

支持 @ 人：

```json
{
  "text": "你好 <at user_id=\"ou_xxx\">张三</at>"
}
```

@ 全员：

```json
{
  "text": "<at user_id=\"all\"></at> 大家看一下"
}
```

自研客户端建议把文本解析成两层：

```json
{
  "plain_text": "你好 张三",
  "segments": [
    { "type": "text", "text": "你好 " },
    { "type": "mention", "user_id": "ou_xxx", "name": "张三" }
  ]
}
```

这样既能原样兼容飞书，也便于前端高亮 @。

---

## 3.2 post：富文本消息

飞书富文本常用于 Markdown 转换后的结构。

```json
{
  "msg_type": "post",
  "content": "{\"zh_cn\":{\"title\":\"标题\",\"content\":[[{\"tag\":\"text\",\"text\":\"正文\"}]]}}"
}
```

真实内容对象：

```json
{
  "zh_cn": {
    "title": "标题",
    "content": [
      [
        {
          "tag": "text",
          "text": "正文"
        }
      ]
    ]
  }
}
```

结构说明：

- `zh_cn` / `en_us`：多语言内容。
- `title`：标题，可选。
- `content`：二维数组。
  - 第一层：行。
  - 第二层：同一行内的多个元素。

常见富文本元素：

```json
{ "tag": "text", "text": "普通文字" }
```

```json
{ "tag": "a", "text": "链接文字", "href": "https://example.com" }
```

```json
{ "tag": "at", "user_id": "ou_xxx", "user_name": "张三" }
```

```json
{ "tag": "img", "image_key": "img_xxx" }
```

自研客户端建议内部统一成块结构：

```json
{
  "type": "rich_text",
  "title": "标题",
  "blocks": [
    {
      "type": "paragraph",
      "children": [
        { "type": "text", "text": "正文" }
      ]
    }
  ]
}
```

发送到飞书时再转换成 `post.content` 二维数组。

---

## 3.3 image：图片消息

飞书格式：

```json
{
  "msg_type": "image",
  "content": "{\"image_key\":\"img_xxx\"}"
}
```

真实内容对象：

```json
{
  "image_key": "img_xxx"
}
```

图片通常需要先上传，得到 `image_key` 后再发送。

自研客户端建议保留：

```json
{
  "msg_type": "image",
  "content": {
    "image_key": "img_xxx",
    "url": "本地或 CDN 地址",
    "width": 800,
    "height": 600,
    "size": 102400,
    "mime": "image/png"
  }
}
```

兼容飞书时，只发送 `image_key`。

---

## 3.4 file：文件消息

飞书格式：

```json
{
  "msg_type": "file",
  "content": "{\"file_key\":\"file_xxx\"}"
}
```

自研客户端建议：

```json
{
  "msg_type": "file",
  "content": {
    "file_key": "file_xxx",
    "name": "report.pdf",
    "size": 204800,
    "mime": "application/pdf",
    "url": "下载地址"
  }
}
```

文件消息应支持：

- 文件名展示。
- 大小展示。
- 下载状态。
- 过期/权限错误提示。

---

## 3.5 audio：音频消息

飞书格式：

```json
{
  "msg_type": "audio",
  "content": "{\"file_key\":\"file_xxx\"}"
}
```

自研客户端建议：

```json
{
  "msg_type": "audio",
  "content": {
    "file_key": "file_xxx",
    "duration": 12.5,
    "mime": "audio/opus",
    "url": "下载地址"
  }
}
```

建议额外保留：

- 播放进度。
- 语音转文字结果。
- 是否已播放。

---

## 3.6 media：视频消息

飞书视频一般使用 `media` 类型，需要视频文件和封面图。

```json
{
  "msg_type": "media",
  "content": "{\"file_key\":\"file_xxx\",\"image_key\":\"img_xxx\"}"
}
```

自研客户端建议：

```json
{
  "msg_type": "media",
  "content": {
    "file_key": "file_xxx",
    "cover_image_key": "img_xxx",
    "url": "视频地址",
    "cover_url": "封面地址",
    "duration": 30.0,
    "width": 1280,
    "height": 720
  }
}
```

---

## 3.7 interactive：交互式卡片

你当前项目的回复消息使用的就是 `interactive`。

当前代码中的结构约等于：

```json
{
  "msg_type": "interactive",
  "content": "{\"elements\":[{\"tag\":\"markdown\",\"content\":\"你好\"}]}"
}
```

真实内容对象：

```json
{
  "elements": [
    {
      "tag": "markdown",
      "content": "你好"
    }
  ]
}
```

更完整的卡片可以包含标题、按钮、分栏、图片等：

```json
{
  "config": {
    "wide_screen_mode": true
  },
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "任务通知"
    },
    "template": "blue"
  },
  "elements": [
    {
      "tag": "markdown",
      "content": "**构建完成**\n请查看结果。"
    },
    {
      "tag": "action",
      "actions": [
        {
          "tag": "button",
          "text": {
            "tag": "plain_text",
            "content": "查看详情"
          },
          "type": "primary",
          "url": "https://example.com"
        }
      ]
    }
  ]
}
```

自研客户端如果要保留飞书卡片特性，建议支持：

- `markdown` 文本块。
- `plain_text` 文本。
- `button` 按钮。
- `image` 图片。
- `hr` 分割线。
- `div` 内容块。
- `action` 操作区。
- `select` / `input` 等交互控件可以后续再做。

卡片事件也需要单独设计，例如：

```json
{
  "event_type": "card_action",
  "message_id": "om_xxx",
  "chat_id": "oc_xxx",
  "operator_id": "ou_xxx",
  "action": {
    "tag": "button",
    "value": {
      "action": "open_task",
      "task_id": "123"
    }
  }
}
```

---

## 3.8 share_chat：分享群聊

```json
{
  "msg_type": "share_chat",
  "content": "{\"chat_id\":\"oc_xxx\"}"
}
```

自研客户端可展示为一张会话卡片。

---

## 3.9 share_user：分享联系人

```json
{
  "msg_type": "share_user",
  "content": "{\"user_id\":\"ou_xxx\"}"
}
```

自研客户端可展示为用户名片。

---

## 4. 接收消息事件格式

飞书事件订阅中，收到消息大致是这种结构：

```json
{
  "schema": "2.0",
  "header": {
    "event_id": "xxx",
    "event_type": "im.message.receive_v1",
    "create_time": "1710000000000",
    "token": "xxx",
    "app_id": "cli_xxx",
    "tenant_key": "xxx"
  },
  "event": {
    "sender": {
      "sender_id": {
        "union_id": "on_xxx",
        "user_id": "xxx",
        "open_id": "ou_xxx"
      },
      "sender_type": "user",
      "tenant_key": "xxx"
    },
    "message": {
      "message_id": "om_xxx",
      "root_id": "om_root",
      "parent_id": "om_parent",
      "create_time": "1710000000000",
      "update_time": "1710000000000",
      "chat_id": "oc_xxx",
      "chat_type": "group",
      "message_type": "text",
      "content": "{\"text\":\"你好\"}",
      "mentions": []
    }
  }
}
```

注意字段名差异：

- 发送时通常叫 `msg_type`。
- 事件里可能叫 `message_type`。
- 你的本地代码封装后使用的是 `msg.content.get("msg_type")`，说明中间层可能已经做了转换。

自研客户端建议统一内部字段为：

```json
{
  "event_type": "message.created",
  "message": {
    "message_id": "msg_xxx",
    "chat_id": "chat_xxx",
    "sender_id": "user_xxx",
    "msg_type": "text",
    "content": {
      "text": "你好"
    },
    "raw_content": "{\"text\":\"你好\"}",
    "create_time": 1710000000000
  }
}
```

这样客户端内部不被飞书字段差异污染。

---

## 5. 回复消息格式

### 5.1 普通回复

```json
{
  "message_id": "om_parent",
  "msg_type": "text",
  "content": "{\"text\":\"这是回复\"}"
}
```

响应：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "message_id": "om_reply",
    "thread_id": "omt_xxx",
    "root_id": "om_parent",
    "parent_id": "om_parent",
    "chat_id": "oc_xxx",
    "create_time": "1710000000000"
  }
}
```

### 5.2 话题内回复

如果 `reply_in_thread=true`，回复会进入话题。

建议内部结构：

```json
{
  "action": "reply_message",
  "parent_message_id": "msg_parent",
  "reply_in_thread": true,
  "msg_type": "text",
  "content": {
    "text": "继续讨论"
  }
}
```

响应：

```json
{
  "ok": true,
  "message": {
    "message_id": "msg_reply",
    "parent_id": "msg_parent",
    "root_id": "msg_root",
    "thread_id": "thread_xxx"
  }
}
```

---

## 6. 更新/编辑消息格式

飞书编辑消息时通常只能编辑自己发出的消息，且不同类型支持情况不一样。你当前项目主要更新交互式卡片。

请求结构：

```json
{
  "message_id": "om_xxx",
  "content": "{\"elements\":[{\"tag\":\"markdown\",\"content\":\"新的内容\"}]}"
}
```

自研客户端建议统一为：

```json
{
  "action": "message.update",
  "message_id": "msg_xxx",
  "new_content": {
    "text": "新的内容"
  },
  "edit_time": 1710000000000
}
```

消息对象保留：

```json
{
  "updated": true,
  "update_time": 1710000000000,
  "edit_history": [
    {
      "content": { "text": "旧内容" },
      "update_time": 1709999999000
    }
  ]
}
```

如果不想保存完整编辑历史，也至少保留 `updated=true` 和 `update_time`，用于前端显示“已编辑”。

---

## 7. 撤回/删除消息格式

飞书撤回后，消息通常不应继续展示原内容。

事件建议：

```json
{
  "event_type": "message.deleted",
  "message_id": "msg_xxx",
  "chat_id": "chat_xxx",
  "operator_id": "user_xxx",
  "delete_time": 1710000000000
}
```

消息状态：

```json
{
  "message_id": "msg_xxx",
  "deleted": true,
  "deleted_time": 1710000000000,
  "content": null
}
```

前端展示：

> 这条消息已撤回

如果是管理员删除他人消息，可以额外保留：

```json
{
  "delete_reason": "admin_delete"
}
```

---

## 8. 表情回应 Reaction 格式

飞书支持给消息添加 emoji reaction。

建议消息对象中保留：

```json
{
  "message_id": "msg_xxx",
  "reactions": [
    {
      "emoji_type": "OK",
      "count": 2,
      "users": [
        { "user_id": "user_a", "name": "张三" },
        { "user_id": "user_b", "name": "李四" }
      ],
      "me_reacted": true
    }
  ]
}
```

新增 reaction 事件：

```json
{
  "event_type": "reaction.created",
  "message_id": "msg_xxx",
  "chat_id": "chat_xxx",
  "operator_id": "user_xxx",
  "reaction": {
    "emoji_type": "OK"
  }
}
```

删除 reaction 事件：

```json
{
  "event_type": "reaction.deleted",
  "message_id": "msg_xxx",
  "chat_id": "chat_xxx",
  "operator_id": "user_xxx",
  "reaction": {
    "emoji_type": "OK"
  }
}
```

---

## 9. 已读信息格式

飞书可以查询消息已读用户。自研客户端如果要模仿，应区分：

- 单聊：显示已读/未读。
- 群聊：显示已读人数、未读人数，可展开列表。

建议结构：

```json
{
  "message_id": "msg_xxx",
  "read_status": {
    "read_count": 8,
    "unread_count": 3,
    "read_users": [
      {
        "user_id": "user_xxx",
        "read_time": 1710000000000
      }
    ]
  }
}
```

单聊可简化：

```json
{
  "message_id": "msg_xxx",
  "read_status": "read | unread"
}
```

---

## 10. 资源下载格式

图片、文件、音频、视频消息通常不会把二进制放在消息里，而是使用资源 key。

下载请求：

```json
{
  "message_id": "om_xxx",
  "file_key": "img_xxx 或 file_xxx",
  "resource_type": "image | file | audio | media"
}
```

下载响应可以是二进制，也可以返回临时 URL：

```json
{
  "ok": true,
  "resource": {
    "key": "file_xxx",
    "url": "https://cdn.example.com/temp/file",
    "expires_at": 1710003600000,
    "mime": "application/pdf",
    "size": 204800
  }
}
```

自研客户端建议：

- 消息里保存资源 key。
- 真实文件通过资源接口拉取。
- URL 设置过期时间。
- 支持本地缓存。

---

## 11. 错误响应格式

飞书 API 常见错误响应：

```json
{
  "code": 99991663,
  "msg": "permission denied",
  "error": {
    "message": "missing scope",
    "permission_violations": [
      {
        "type": "scope",
        "subject": "im:message"
      }
    ]
  }
}
```

自研客户端建议统一错误格式：

```json
{
  "ok": false,
  "code": "permission_denied",
  "message": "没有发送消息权限",
  "details": {
    "missing_scopes": ["im:message"]
  },
  "retryable": false
}
```

常见错误类型：

| 错误 | 含义 | 是否可重试 |
|---|---|---|
| `permission_denied` | 权限不足 | 否 |
| `chat_not_found` | 会话不存在或无权限访问 | 否 |
| `user_not_found` | 用户不存在 | 否 |
| `message_not_found` | 消息不存在 | 否 |
| `rate_limited` | 频率限制 | 是 |
| `invalid_content` | 消息内容格式错误 | 否 |
| `resource_expired` | 文件资源过期 | 可重新获取 |
| `internal_error` | 服务内部错误 | 是 |

---

## 12. 推荐的内部消息模型

如果要做一个仿飞书客户端，建议内部不要直接使用飞书原始结构，而是设计稳定的领域模型。

```json
{
  "id": "msg_xxx",
  "chat_id": "chat_xxx",
  "thread_id": "thread_xxx",
  "root_id": "msg_root",
  "parent_id": "msg_parent",
  "sender": {
    "id": "user_xxx",
    "type": "user | bot | app",
    "name": "张三",
    "avatar": "https://..."
  },
  "type": "text | rich_text | image | file | audio | video | card | share_chat | share_user",
  "content": {},
  "raw": {
    "provider": "feishu",
    "msg_type": "text",
    "content": "{\"text\":\"你好\"}"
  },
  "mentions": [],
  "reactions": [],
  "read_status": null,
  "reply_count": 0,
  "status": "sending | sent | failed | deleted",
  "created_at": 1710000000000,
  "updated_at": 1710000000000,
  "deleted_at": null
}
```

这样有几个好处：

1. 前端展示不依赖飞书原始字段。
2. 后端可以接入飞书、微信、钉钉、自研协议等多个来源。
3. 仍然通过 `raw` 保留飞书兼容信息。
4. 后续支持编辑、撤回、reaction、话题会更容易。

---

## 13. 前端渲染建议

按消息类型分组件：

| 类型 | 组件 |
|---|---|
| `text` | TextMessage |
| `rich_text` | RichTextMessage |
| `image` | ImageMessage |
| `file` | FileMessage |
| `audio` | AudioMessage |
| `video` | VideoMessage |
| `card` | CardMessage |
| `share_chat` | ChatShareMessage |
| `share_user` | UserShareMessage |

每条消息通用展示：

- 头像。
- 昵称。
- 时间。
- 发送状态。
- 已编辑标记。
- 撤回状态。
- reaction 区域。
- 回复入口。
- 更多操作菜单。

---

## 14. 最小可实现版本

如果先做 MVP，建议保留这些能力：

1. `text` 文本消息。
2. `image` 图片消息。
3. `file` 文件消息。
4. `card` / `interactive` 卡片消息，只支持 markdown + button。
5. `reply` 回复消息。
6. `thread_id` 话题。
7. `reaction` 表情回应。
8. `message.update` 编辑。
9. `message.deleted` 撤回。
10. `read_status` 已读状态。

可以暂缓：

- 多语言富文本。
- 复杂卡片布局。
- 音视频转码。
- 合并转发。
- Pin 消息。
- 群管理。

---

## 15. 和当前 miniClaw 代码的对应关系

当前项目里：

- 飞书 API 封装在 `feishu/feishu_api.py`。
- 收到消息后的解析在 `link_service.py`。
- 当前接收逻辑主要识别：
  - `text`
  - `image`
  - `post`
- 当前回复逻辑使用：
  - `interactive` 卡片消息
  - `elements.markdown`
- 当前更新消息逻辑只适合更新卡片消息。

也就是说，当前代码已经具备“卡片流式回复”的雏形，但如果要完整模仿飞书客户端，需要补足：

1. `interactive` 消息的接收解析。
2. 文件、音频、视频消息解析。
3. reaction 状态同步。
4. 消息编辑事件处理。
5. 撤回事件处理。
6. read receipt 已读状态。
7. thread/message 双层消息列表模型。

---

## 16. 设计原则

仿飞书客户端时建议遵循：

1. **内部模型稳定，外部协议适配**  
   不要让前端直接依赖飞书原始字段。

2. **content 用对象保存，raw 保留原始字符串**  
   便于渲染，也便于调试。

3. **消息状态独立于消息内容**  
   `sending` / `sent` / `failed` / `deleted` 不要混在 `content` 里。

4. **资源消息只保存 key，不保存大文件**  
   图片、文件、音视频走资源接口。

5. **话题是一等能力**  
   `thread_id` 不要当普通字段随便丢掉，否则后续很难做飞书式话题回复。

6. **卡片消息需要单独渲染引擎**  
   不要把卡片粗暴转文本，否则会丢失按钮、布局、交互能力。
