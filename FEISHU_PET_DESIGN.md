# DyberPet-Lark 飞书桌面伴侣 设计文档

## 1. 产品构思与核心价值

### 产品定位

将 DyberPet 桌面宠物改造为飞书工作场景的"智能助理伴侣"——一个常驻桌面、有生命感的 AI 代理前端。它不是另一个通知中心，而是一个有情感反馈、能主动介入的工作伙伴。

### 核心价值主张

**解决的真实痛点：**
- 飞书消息量大，重要消息容易被淹没
- 跨群/跨会话的上下文切换成本高
- 下班前需要手动整理今日待办，费时费力
- AI 回复建议分散在各个对话窗口，缺乏统一入口

**差异化优势：**
- 宠物形象提供情感锚点，降低 AI 工具的疏离感
- 常驻桌面，不依赖飞书窗口是否激活
- 晚间复盘是主动行为，而非被动查询

---

## 2. 技术链路

### 整体数据流

```
飞书服务器
    │
    │ WebSocket 长连接 (lark-event)
    ▼
miniClaw 后端 (FastAPI + WebSocket)
    │  ├── 事件接收与路由
    │  ├── Claude/OpenAI function calling
    │  └── 消息历史存储 (history.json / SQLite)
    │
    │ WebSocket ws://localhost:18888/ws/pet
    ▼
DyberPet 桌面客户端 (PySide6)
    │  ├── 宠物动画引擎
    │  ├── 气泡通知组件
    │  ├── 快捷操作面板
    │  └── 晚间复盘窗口
```

### 事件协议（miniClaw → DyberPet）

```json
// 新消息 + AI 建议
{
  "type": "message",
  "sender": "张三",
  "chat_name": "产品讨论群",
  "content": "下午三点开会吗？",
  "suggestion": "好的，准时参加。",
  "actions": [
    { "id": "send", "label": "发送建议回复" },
    { "id": "later", "label": "稍后处理" },
    { "id": "ignore", "label": "忽略" }
  ],
  "priority": "normal"
}

// 晚间复盘报告
{
  "type": "review",
  "summary": "今日共 23 条消息，3 条未回复，2 个待办事项。",
  "unreplied": [...],
  "todos": [...]
}
```

### 快捷操作 API（DyberPet → miniClaw）

```
POST /actions/execute
{ "type": "send_message", "chat_id": "...", "text": "好的，准时参加。" }

POST /review/daily
→ 返回当日复盘报告
```

---

## 3. 模块划分

### miniClaw 后端新增

| 模块 | 文件 | 职责 |
|------|------|------|
| Pet 推送网关 | `main.py` 新增端点 | 维护 DyberPet WS 连接，广播事件 |
| 消息分析 | `link_service.py` 扩展 | 接收消息后调用 Claude 生成建议 |
| 快捷操作 API | `main.py` 新增端点 | 代理执行飞书操作（发消息、建任务） |
| 复盘 API | `main.py` 新增端点 | 聚合今日消息，调用 Claude 生成复盘 |

### DyberPet 客户端新增

| 模块 | 文件 | 职责 |
|------|------|------|
| miniClaw 连接客户端 | `DyberPet/miniclaw_client.py` | WS 连接、断线重连、事件分发 |
| 智能气泡组件 | 基于 `Notification.py` 扩展 | 富文本气泡 + 操作按钮 |
| 复盘窗口 | `DyberPet/review_window.py` | 展示复盘报告、待办处理 |
| 宠物状态扩展 | `DyberPet/modules.py` 扩展 | 新增 alert / waiting_reply 动画状态 |

---

## 4. 最终预期效果

用户在使用飞书工作时，桌面角落的宠物作为一个始终在线的外设存在。

**实时消息**：当新消息到来，宠物做出反应动画（抬头、竖耳），屏幕边缘弹出气泡，显示 AI 生成的回复建议和 2-3 个快捷按钮。用户无需切换到飞书窗口，单击按钮即可完成回复。对于高优先级消息（@我、紧急关键词），宠物展示更明显的提醒动画。

**晚间复盘**：每晚 21:00，宠物主动弹出复盘窗口。展示今日消息摘要、标红的未回复消息、AI 提取的待办项，以及对话中提到的承诺或 deadline。用户可以逐项处理，或一键将所有待办推送到飞书任务清单。

核心体验：宠物是 AI 能力的具象化载体，它"知道"你今天经历了什么，并在合适的时机主动提醒你。

---

## 5. 开发计划

### Phase 1 — 基础通信管道（Week 1-2）

**目标**：打通 miniClaw 与 DyberPet 的数据通路。

**任务**：
- [ ] miniClaw 新增 `ws://localhost:18888/ws/pet` 端点
- [ ] DyberPet 新增 `miniclaw_client.py`，实现连接、断线重连
- [ ] 定义并实现事件协议
- [ ] miniClaw 接收飞书消息后转发到宠物（先转发原始消息，不带 AI 建议）

**验收标准**：飞书收到任意消息，DyberPet 气泡弹出消息内容；断网重连后数据流恢复。

---

### Phase 2 — AI 建议 + 快捷操作（Week 3-4）

**目标**：AI 回复建议在宠物侧可见，用户能通过宠物执行简单操作。

**任务**：
- [ ] miniClaw `link_service.py` 分析消息后附带 suggestion + actions
- [ ] 快捷操作 API（发消息）
- [ ] DyberPet 气泡扩展支持操作按钮
- [ ] 宠物新增 `alert` 动画状态（@我触发）

**验收标准**：收到飞书消息后 3 秒内气泡显示 AI 建议；点击"发送"按钮飞书收到消息。

---

### Phase 3 — 晚间复盘（Week 5-6）

**目标**：复盘窗口可用，能准确标出未处理事项。

**任务**：
- [ ] miniClaw 消息历史持久化（保留已有 history.json 机制，扩展字段）
- [ ] 复盘 API + Claude prompt 调优
- [ ] DyberPet 复盘窗口 UI
- [ ] 定时触发（默认 21:00，可配置）
- [ ] 待办一键同步到飞书任务

**验收标准**：每天 21:00 自动弹出复盘；未回复消息识别准确率 > 80%；飞书任务同步成功。

---

### Phase 4 — 打磨收尾（Week 7）

**目标**：体验流畅，可日常使用。

**任务**：
- [ ] 托盘菜单扩展（连接状态、待处理角标）
- [ ] 错误处理与降级（API 超时、连接断开友好提示）
- [ ] 用户可配置项：复盘时间、通知过滤规则
- [ ] 端到端集成测试（连续 5 个工作日）

**验收标准**：连续 5 个工作日无崩溃；所有配置项可访问。
